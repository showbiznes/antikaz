# =============================================================================
# requirements.txt — оптимизировано для bothost.ru (Linux, Docker, CPU-only)
# =============================================================================

# Discord
discord.py==2.3.2

# Обработка изображений
Pillow==10.3.0

# HTTP клиент
aiohttp==3.9.5

# CLIP zero-shot детекция
openai-clip==1.0.1
ftfy==6.1.3
regex==2024.4.28

# OCR (tesseract ставится через Dockerfile)
pytesseract==0.3.10

# Переменные окружения (.env файл)
python-dotenv==1.0.1
