# =============================================================================
# requirements.txt — оптимизировано для bothost.ru (Linux, Docker, CPU-only)
# =============================================================================

# Discord
discord.py==2.3.2

# PyTorch CPU (для Linux bothost.ru — без +cpu суффикса)
# Устанавливается через команду в startup.sh, а не через pip напрямую
# (см. startup.sh)

# Обработка изображений
Pillow==10.3.0

# HTTP клиент
aiohttp==3.9.5

# CLIP zero-shot детекция
# Автоматически скачивает ~350 МБ весов при первом запуске
openai-clip==1.0.1
ftfy==6.1.3
regex==2024.4.28

# OCR (tesseract нужно ставить на уровне OS — см. Dockerfile)
pytesseract==0.3.10
