# =============================================================================
# requirements.txt — для bothost.ru
# ВАЖНО: torch/torchvision/CLIP убраны — на bothost.ru их не удаётся поставить
# (нет поддержки --index-url, обычная PyPI-сборка torch слишком тяжёлая и
# билд не успевает её скачать). Бот работает на OCR + анализе цвета —
# оба метода не требуют torch и работают уже сейчас.
# =============================================================================

# Discord
discord.py==2.3.2

# Обработка изображений
Pillow==10.3.0

# HTTP клиент
aiohttp==3.9.5

# Поиск ключевых слов в тексте
ftfy==6.1.3
regex==2024.4.28

# OCR — основной метод детекции на bothost.ru
pytesseract==0.3.10

# Переменные окружения
python-dotenv==1.0.1
