# =============================================================================
# requirements.txt — для bothost.ru
# ВАЖНО: сборка на bothost.ru ставит пакеты ПОСТРОЧНО (pip install "$line" для
# каждой строки отдельно), поэтому флаги вроде --extra-index-url НЕ работают —
# каждая строка должна быть валидным одиночным package-spec без пробелов.
# torch здесь ставится с обычного PyPI (без --index-url на CPU-wheel), это
# больше по размеру, но работает без ошибок на CPU-хостинге.
# =============================================================================

# Discord
discord.py==2.3.2

# Обработка изображений
Pillow==10.3.0

# HTTP клиент
aiohttp==3.9.5

# Поиск ключевых слов в тексте
ftfy==6.1.3
regex==2024.4.28

# OCR — основной метод детекции на bothost.ru
pytesseract==0.3.10

# Переменные окружения
python-dotenv==1.0.1

# PyTorch — обязательно для EfficientNet (обычная сборка с PyPI, без --index-url)
torch==2.3.1
torchvision==0.18.1

# CLIP (zero-shot детекция — работает без обучения)
openai-clip==1.0.1
